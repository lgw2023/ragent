Academic conceptual research figure, white background, flat 2D vector infographic, top-tier venue quality (NeurIPS / ACL / SIGIR style), highly abstract, distilled, minimal text, balanced modular composition, thin outlines, dotted grouping borders, clean arrows, readable metric badges, balanced whitespace, restrained color palette, no visual clutter.

Aspect ratio: 16:9 landscape.

Core idea - one sentence the figure must communicate:
RAgent turns multimodal professional documents into provenance-grounded Entity-Relation-Chunk evidence graphs, retrieves evidence through structure-aware hybrid search and reranking, accelerates a 24 GB FoodScience knowledge graph with FAISS/HNSW sidecars, and validates the gains with auditable evidence coverage, QA correctness, latency, and cross-system benchmark results.

Composition:
5 left-to-right panels, with a thin bottom ribbon for empirical evidence badges. Panels 1-3 are the method core and should be visually dominant. Panel 4 is an acceleration panel. Panel 5 is an evaluation and traceability panel. The figure should read as one continuous framework, not five isolated diagrams. Use repeated visual grammar: evidence cards, provenance tags, heterogeneous graph nodes, ranked candidate stacks, and metric badges.

Main Panel 1 - "Evidence Ingestion" (green):
Show heterogeneous professional-document inputs entering a structure-aware evidence-unit pipeline.

Left side:
- Four compact source icons: PDF, Image, Table, Wide Table.

Middle:
- "AST Chunk": a small document tree splitting into ordered text chunks.
- "Row Split": a wide table decomposed into row cards.
- "Image Grounding": an image linked to a compact description card.
- "Source Tags": a provenance badge attached to every unit.

Right side:
- A clean stack of uniform evidence cards, each with a tiny provenance tag badge.
- Use small visual hints for page, section, and source, but do not write field names such as page, chunk_id, or source_ref.

Visual message:
The input is not flattened into anonymous text chunks; it becomes structured, multimodal, provenance-aware evidence units.

Main Panel 2 - "ERC Evidence Graph" (blue + purple):
This is the visual anchor of the figure. Render a heterogeneous two-layer graph with clear provenance binding.

Upper semantic layer:
- 6-8 Entity nodes as blue circles with slightly varied size.
- 5-7 Relation edges as purple directed arrows, with subtle thickness differences to imply typed or weighted relationships.
- A few relation edges may carry tiny abstract ticks, not long labels.

Lower evidence layer:
- 4-5 Chunk nodes as rounded rectangles, each with a small provenance tag badge.
- Thin gray connector edges bind chunks upward to the entities and relations they support.
- Show one chunk grounding multiple entities and one entity grounded by multiple chunks.

Below the graph:
- Three aligned index icons for entity index, relation index, and chunk index.
- Faint dotted lines connect each index to its corresponding node type.

Visual message:
The knowledge graph is not an abstract triple store; it is an evidence-grounded graph where semantic nodes remain linked to original document chunks.

Main Panel 3 - "Hybrid Evidence Retrieval" (orange + red):
Show a query entering from the left and becoming ranked, fused, reranked, and selected evidence.

Left:
- One compact query token icon splitting into 2-3 variant cards.

Middle:
- Parallel candidate streams:
  1. chunk vector stream
  2. entity vector stream
  3. relation vector stream
  4. graph neighborhood expansion stream
- Each stream is a short ranked candidate stack with tiny rank and score badges, such as #1 .92, #2 .87, #3 .81. These numeric badges are allowed and should remain small.

Convergence:
- Candidate stacks merge into a fusion shape.
- Then pass through a rerank bar where two candidates visibly swap order.
- Then pass through a red "coverage gate" that keeps a compact top-k evidence set.

Right:
- Selected ordered evidence cards flow to Panel 5.

Visual message:
Retrieval is multi-channel and structure-aware: chunk, entity, relation, graph expansion, fusion, rerank, and coverage-aware final selection all contribute to evidence coverage.

Main Panel 4 - "Scale Acceleration" (gold + teal):
Show server-scale FoodScience knowledge-graph retrieval being accelerated without rebuilding the knowledge graph.

Left:
- A large graph/index store icon with many faint nodes and edges in the background, labeled "FoodScience KG".
- Add three compact scale badges around the graph store: "24 GB", "6,520 docs", "905,580 relations".
- Show three 2560-d vector index cylinders labeled only "chunks", "entities", "relationships"; make the relationship cylinder visibly largest, with a tiny "12 GB" badge.
- A small sidecar branch labeled "FAISS/HNSW" attaches to the entity and relationship indexes.

Middle:
- Two parallel acceleration lanes:
  1. "Relation Search": slow Nano bar shrinks into a very short HNSW bar.
  2. "Retrieval-only": long Nano path compresses into a shorter FAISS/HNSW path.
- Use small metric badges near the bars:
  - "54.15s -> 0.018s"
  - "-99.97%"
  - "75.76s -> 6.59s"
  - "-91.3%"
  - "11.5x"
  - "P95 7.33s"

Right:
- A small "10/10 evidence" check badge appears at the output, with a tiny "no cache hit" mark to indicate the measurement is not caused by cache reuse.

Visual message:
Large-scale retrieval is accelerated by sidecar vector indexes and HNSW search, especially for the near-900k relationship vector store, while preserving stable evidence chunk recall.

Main Panel 5 - "Traceable QA + Benchmark" (purple + slate):
Combine traceable answering with empirical validation, but keep the panel compact.

Top:
- Selected evidence cards feed into an answer block.
- Thin colored trace lines connect the answer back to evidence cards and graph nodes.
- A small multi-hop loop icon suggests evidence composition and reasoning.

Bottom:
- A compact benchmark comparison strip with four small score cards:
  - RAgent-FAISS: "7.72 / 7.46"
  - RAgent: "7.71 / 6.98"
  - enhanced Milvus: "6.78 / 4.90"
  - baseline Milvus: "5.67 / 4.74"
- Add a tiny caption-like label "LLM / Rule" near the score cards.
- Add one evidence-quality badge: "2.80/3 evidence" and "+177% vs enhanced".

Visual message:
The framework produces answers with traceable evidence and is validated against competing systems on real health-diet benchmark questions.

Bottom empirical ribbon:
A thin horizontal ribbon runs below all panels, containing small, evenly spaced metric badges. These are the only dense numbers in the figure and should remain visually secondary.

Metric badges to include:
- "186 questions"
- "331 evidence links"
- "no fake provenance"
- "FoodScience 24 GB"
- "6,520 docs"
- "58,184 chunks"
- "905,580 relations"
- "coverage +34.1%"
- "Recall@K +51.8%"
- "B7 coverage +47.3%"
- "QA correctness +8.4%"
- "retrieval -91.3%"
- "relation -99.97%"
- "10/10 evidence"
- "Rule lead 8/8"
- "LLM lead 6/8"

Make these badges look like lightweight evaluation stamps, not a table. Use tiny icons: checkmark for provenance, upward arrow for gains, stopwatch for latency, trophy or rank mark for system comparison. Keep all icons abstract and simple.

Label budget:
Maximum ~38 visible text labels across the main panels, excluding tiny numeric metric badges. No sentences inside the figure, no bullet lists, no code terms, no model names, no dense tables. Panel titles count as labels. Metric badges may contain numbers but should be tiny and sparse.

Color:
green = structured ingestion, blue = entities and chunks, purple = relations and traceability, orange = hybrid retrieval, red = final selection gate only, gold = acceleration and sidecars, slate gray = benchmark comparison, light gray = neutral connectors. Use a restrained warm-to-cool progression from left to right.

Negative constraints:
no photorealism, no 3D, no glossy UI, no dashboard, no screenshots, no deployment diagram, no server rack, no raw code, no model names, no crowded arrows, no gradient fills, no decorative ornaments, no dense tables, no fake document screenshots, no marketing hero style.

Important claim boundaries:
- Do not imply the "Full" configuration is the best final system; show B7 as the strongest current retrieval setting.
- Do not imply provenance is generated after the fact; show provenance tags attached from ingestion through graph and answer trace.
- Do not imply FAISS/HNSW rebuilds the knowledge graph; show it as a sidecar acceleration layer over existing indexes.
- Do not show cross-system results as training performance; show them as benchmark score cards.

Desired impression:
A reader should grasp the full contribution in 10-15 seconds: multimodal evidence ingestion, provenance-grounded ERC graph, hybrid evidence retrieval, large-scale FAISS/HNSW acceleration, and auditable benchmark superiority. The figure should feel rigorous, sparse, and publication-ready, with method and evidence balanced in one clean framework diagram.
