Academic conceptual research figure, white background, flat 2D vector infographic, top-tier venue quality (NeurIPS / ACL style), highly abstract, distilled, minimal text, balanced modular composition, thin outlines, dotted grouping borders, clean arrows, balanced whitespace, restrained color palette, no visual clutter.

Aspect ratio: 16:9 landscape.

Core idea — one sentence the figure must communicate:
Professional documents become provenance-aware evidence units, organized as an Entity-Relation-Chunk heterogeneous evidence graph, retrieved through cache-accelerated hybrid selection, and answered with multi-hop provenance traceability.

Composition:
4 panels, left to right, not equal visual weight. Each panel has one bold title and a small number of abstract visual elements. No panel should feel denser or more complex than the others. Prefer shapes, spatial relationships, and color over text. The figure should read as a single flowing transformation, not four independent diagrams.

Main Panel 1 — "Evidence Construction" (green) :
Abstract document icons (PDF, image, table) on the left, flowing through a transformation boundary into a neat stack of evidence cards on the right. Each card carries a tiny provenance tag mark. Below, show two parallel lanes ("Export" and "Replay") converging into a single commit gate, implying concurrent and resumable construction. Keep it schematic — no processing sub-steps, no internal algorithm names.

Main Panel 2 — "ERC Evidence Graph" (blue + purple):
A compact heterogeneous node-link graph at center. Three visually distinct node types: Entity (blue circles), Relation (purple links), Chunk (blue rounded rectangles). The graph should feel like a unified evidence network where semantic structure and source text are explicitly bound. Below, three small aligned index icons representing per-type vector indexes. This panel is the visual anchor of the figure.

Main Panel 3 — "Hybrid Retrieval" (orange, red gate):
A query splits into variant cards, with a cache chip providing a fast-path shortcut. Multiple retrieval streams (vector search + graph expansion) converge into a fusion shape, pass through a rerank bar, then a red selection gate. The gate visually implies coverage and diversity. Output: a compact set of selected evidence cards. Show the flow, not the internals.

Minor Panel 4 — "Traceable QA" (purple):
Upper region: a question decomposes into sub-questions with iterative solving arrows — multi-hop reasoning shown as a compact loop. Lower region: subtle colored trace lines connecting the answer back through evidence cards to graph nodes, forming a provenance chain. The key message: every answer links back to its source evidence and graph structure.

Label budget:
Maximum ~20 visible text labels across the entire figure. Panel titles count. No sentences, no bullet lists, no code terms, no algorithm names, no metadata field names.

Color:
green = evidence construction, blue = entities and chunks, purple = relations and traceability, orange = retrieval and fusion, red = selection gate only, gray = neutral connectors. Restrained, semantically clear, visually harmonious.

Negative constraints:
no photorealism, no 3D, no glossy UI, no screenshots, no dashboard, no deployment diagram, no engineering blueprint, no crowded arrows, no gradient fills, no decorative icons, no implementation details, no algorithm name labels, no cache internals, no runtime packaging.

Desired impression:
A reader grasps the entire framework in 10 seconds. Elegant, spare, conceptual, balanced — the kind of figure that makes a reviewer think "they know exactly what matters."
