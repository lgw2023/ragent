Academic research figure, white background, flat 2D vector infographic, top-tier journal style, highly abstract, distilled, minimal text, elegant modular composition, thin outlines, balanced whitespace, clean arrows, no visual clutter.

Aspect ratio: 16:9 landscape.

Core goal:
Create a concise conceptual figure for the latest Ragent algorithmic framework. The figure should communicate one main idea while still hinting at the main algorithmic improvements:

Professional documents are transformed into provenance-aware evidence units, organized as an Entity-Relation-Chunk evidence graph, retrieved through structure-aware hybrid evidence selection, and returned as traceable answers.

Secondary ideas must be visible as compact visual anchors, not expanded sub-flows:
- concurrent evidence extraction and graph materialization
- replayable / revision-aware graph build
- keyword candidate caching for faster repeated retrieval

This must look like a high-level research contribution figure, not a system architecture diagram.

Composition:
Use 4 large panels arranged left to right. Each panel should be visually simple, with one bold title and at most 3 internal labels. Keep text extremely sparse. Prefer symbols, shapes, color, and spatial relationships over explanations. Do not include a separate Flat Chunk RAG baseline panel.

Panel 1 - "Structured Evidence" (green):
Show a small cluster of professional document inputs: PDF, image, table, wide table.
Transform them into a neat stack of evidence cards.
Each card has a tiny source tag mark, but do not write detailed metadata.
Must show three visible parallel build lanes converging into one compact merge/commit gate before the ERC graph arrow. This is the visual anchor for concurrent graph construction and controlled materialization.
Add a small circular revision arrow near the merge gate to suggest replayable / revision-aware build.

Visual motifs:
- document icons entering a clean extraction funnel
- output evidence cards with small provenance tags
- three parallel build lanes
- compact merge/commit gate with a small revision arrow

Allowed labels in this panel:
"Documents", "Parallel Build", "Commit"

Panel 2 - "ERC Evidence Graph" (blue and purple):
Show a compact heterogeneous graph as the visual center.
Use blue circles for Entity nodes, purple links for Relation edges, and blue rounded rectangles for Chunk evidence nodes.
Make Chunk nodes visibly connected to both entities and relations, so the graph clearly binds semantic structure to source evidence.

Below the graph, show three tiny aligned index icons, but keep them visually secondary.

Allowed labels in this panel:
"Entity", "Relation", "Chunk"
"Vector Indexes" may appear once below the three index icons.

Panel 3 - "Hybrid Retrieval" (orange):
Show a query entering three abstract retrieval streams:
1. chunk evidence search
2. entity / relation search
3. graph neighborhood expansion

The streams converge into one compact fusion shape, then pass through a small red selection gate.
The selection gate should visually imply coverage, diversity, and quality filtering without explaining them in text.

Must show a visible keyword-cache shortcut near the query path: query variants pass through a small cache chip, with one short arrow bypassing repeated vector search and reusing candidate cards. This is the visual anchor for keyword candidate cache acceleration. Do not show cache layers or cache internals.

Allowed labels in this panel:
"Query", "Keyword Cache", "Selection"

Panel 4 - "Traceable Answer" (purple):
Show selected evidence cards flowing into an answer block.
Draw subtle colored trace lines from the answer back to evidence cards and graph nodes.
The key visual message is answer-to-evidence provenance.

Allowed labels in this panel:
"Evidence", "Answer", "Trace"

Overall visual message:
The figure should show a clean transformation:

Documents -> Evidence Units -> ERC Graph -> Hybrid Selection -> Traceable Answer

Only this high-level path needs to be obvious. Concurrent build and keyword cache must be visibly present as compact acceleration / reliability motifs, but must not dominate the figure.

Strict text budget:
- Maximum total visible text labels: 18.
- No sentence labels inside the figure.
- No bullet lists inside the figure.
- No code terms.
- No long metadata labels such as page_number, source_ref, index_digest, corpus_revision, GLiNER, rerank, SQLite, MEP, Qwen3, strict replay, or query cache.
- It is acceptable to represent these ideas visually as tiny icons, but not as text.

Visual language:
- use simple cards, graph nodes, tags, arrows, index cylinders, cache icon, and trace lines
- use repeated geometry across the whole figure
- keep all arrows thin and sparse
- keep each panel breathable
- use fewer elements than a normal architecture diagram
- make the figure look like a distilled algorithmic abstraction

Color style:
Use restrained semantic colors:
- green for structured evidence
- blue for entities and chunks
- purple for relations and traceability
- orange for retrieval and fusion
- red only for the final selection gate
- gray for baseline connectors and neutral structure

Avoid saturated neon colors, gradients, 3D, shadows, glossy effects, decorative backgrounds, and dense icon packs.

Negative constraints:
no photorealism
no 3D
no glossy UI
no screenshots
no dashboard
no deployment diagram
no dense workflow chart
no engineering blueprint
no tiny unreadable text
no crowded arrows
no cache stack details
no runtime packaging details
no long lists of algorithm names
no over-explaining
no hidden or barely visible parallel-build / keyword-cache motifs

Desired impression:
elegant, sparse, abstract, balanced, research-paper quality. A reader should understand the whole framework in 10 seconds: structured evidence is built from documents, grounded in an ERC graph, selected through hybrid retrieval, and used to produce traceable answers.
