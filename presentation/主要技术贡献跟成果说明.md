基本对应，但原方案第（3）更偏“多跳问题拆解”，和结构图中的 Panel 5 “Traceable QA Workbench”不完全贴合；另外 Panel 1 是基线对照，不适合作为项目方案点。建议把方案改成下面这一版，更贴合 5 个模块的视觉逻辑：

背景：专业领域知识服务正从扁平文本检索走向多模态、结构化、可追溯的证据增强推理。

问题：复杂专业文档中的事实证据跨文本、图像、表格、页面层级与实体关系分布，传统 Flat Chunk RAG 难以稳定支撑多跳推理、数值一致性与来源追踪。

目标：将专业文档从非结构化内容转化为可检索、可推理、可追溯的 Entity-Relation-Chunk 证据网络，构建结构感知的专业文档 RAG 系统。

方案：（1）结构化多模态摄取：基于 MinerU 解析 PDF 文档，并将 Markdown AST 分块、图片语义描述回写、表格/宽表行级拆分与 page / section / source_ref 元数据绑定统一建模为结构化证据单元，实现从 PDF、Image、Table、Wide Table 到可追溯 chunk 的证据化转换；（2）ERC 异构证据图构建：通过 LLM 抽取实体、关系和内容关键词，将 Entity、Relation、Chunk 三类节点显式连接，chunk 节点保留页码、章节、来源等 provenance tags，并同步构建实体向量库、关系向量库、chunk 向量库与图存储后端，形成语义结构与原始证据共址的异构证据网络；（3）覆盖感知混合检索与可追溯问答：查询阶段生成 query variants，并行执行 chunk 向量召回、实体召回、关系召回与图谱扩展，通过加权融合、rerank 和 coverage-aware evidence selection 生成 traceable context，同时输出关键词、图谱命中、候选融合、重排结果、证据块与来源引用等 QA trace，用于支撑答案-证据-图节点之间的可检查追踪。

效果：在当前 benchmark 的 7 道肥胖/高血压/膳食指南多跳问答评测中，KG 增强版本平均加权得分达到 8.09/10，5/7 题胜出；Milvus 样例中检索耗时记录为 36.6ms。

系统创新能力：项目围绕“结构化证据单元-ERC 证据图-混合检索-可追溯 QA”形成了较完整的系统抽象，体现出从文档结构、知识结构到推理链路的整体设计能力。

标准/工程/商业转化能力：项目完成了可插拔模型与存储后端、CLI 入口、缓存、进度、重试、状态管理和来源引用输出等工程封装，为专业文档知识库和行业问答场景落地提供了较好的标准化基础。



背景：在专业 PDF、标准文档与医学营养指南等知识密集场景中，生成式问答的核心瓶颈正在从“模型会不会生成”转向“系统能否可靠组织、检索和解释复杂文档证据”。

问题：传统文本块向量检索 RAG 会把章节、表格、图片、实体关系和来源信息压扁为孤立 chunk，导致复合约束问题、多跳证据聚合、数值表格检索和答案溯源能力不足。

目标：本项目目标是构建以 Entity-Relation-Chunk 异构证据图为核心的结构化证据检索系统，将专业文档解析、知识图谱构建、混合检索、证据覆盖选择和可视化问答追溯形成完整闭环。

方案：（1）结构化证据建模与 ingestion pipeline：基于 MinerU 解析、Markdown AST 切块、章节/页码/source_ref 元数据回填、图片多模态描述注入、表格行级拆分与宽表记录建模，将 PDF、图片、表格和宽表统一转化为可追溯证据单元；（2）Entity-Relation-Chunk 异构证据图与混合检索算法：在实体、关系和原文 chunk 之间建立显式来源边，将知识图谱从抽象语义层升级为证据级图结构，并融合 chunk 向量召回、实体向量召回、关系向量召回、图邻域扩散、query variant 拆解、图谱支持度重打分、rerank 与覆盖率导向上下文选择；（3）可解释问答与图谱工作台联动：通过 kg-workbench 将 ragent 的图谱、chunk、文档、图片和 QA trace 映射为可浏览的工作台对象，支持图谱浏览、证据面板、文档定位、问答会话和推理链路展示，使检索结果从“答案文本”扩展为可检查的证据过程。

效果：当前样例知识库构建出 16030 个图节点、56050 条图边、15656 条实体向量、56050 条关系向量和 2610 条 chunk 向量，2335 个入库单元全部 processed；核心混合召回单元测试 8/8 通过，benchmark 样例中一轮评审达到 8.09 vs 7.09、5/7 题胜出，并在 q7 多证据数值推理样例中命中 150kcal、38.5kcal/10min、88kcal/10min 三项关键证据并得到与 groundtruth 一致的结果。

系统创新能力：项目围绕“结构化证据检索”这一主线，将文档解析、异构图建模、混合召回、证据选择和可视化追溯组织成统一系统，体现了从局部算法改进上升到整体检索范式设计的系统抽象能力。

标准/工程/商业转化能力：项目在真实仓库中实现了状态机入库、失败回滚、embedding 回填、多 provider 模型接入、trace 记录、前后端工作台与自动化校验等工程模块，为后续接入专业文档知识管理、可解释问答和行业知识库应用打下了较稳健的工程基础。



## 知识图谱框架、算法创新与实验效果 12 点凝练总结

1. RAgent 的核心创新是将专业文档组织为带 provenance 的 Entity-Relation-Chunk 异构证据图，使答案、实体关系和原文 chunk 能够双向追溯。
2. 系统通过 MinerU 解析、Markdown AST 切块、图片语义描述回写和表格/宽表行级建模，把 PDF、图片、表格统一转化为可检索的结构化证据单元。
3. 检索链路融合 chunk 向量、entity 向量、relation 向量、图邻域扩展、query variants、rerank 和覆盖感知证据选择，相比单路向量检索更适合多约束问题。
4. 在 DQE-mapped 186 题 ERC 消融中，引入实体召回后 required evidence coverage 从 0.4158 提升到 0.5575，相对提升约 34.1%。
5. 加入图邻域扩展后 Evidence Recall@K 从 chunk-only 的 0.3315 提升到 0.5033，相对提升约 51.8%，说明图结构能补足直接语义召回遗漏的证据。
6. B7 配置的 required evidence coverage 达到 0.6123，相比 B0 的 0.4158 相对提升约 47.3%，是当前端到端检索表现最稳健的配置。
7. B7 下游回答 correctness 从 B0 的 0.6879 提升到 0.7456，相对提升约 8.4%，说明检索证据覆盖改善能够传导到问答质量。
8. 评测数据中的 331 条 required evidence 均映射到真实 ragent project chunk，未伪造 page、chunk_id 或 source_ref，保证了实验结论的可审计性。
9. 服务器端 FoodScience_KG_final_sharded 知识图谱规模约 24 GB，覆盖 6,520 篇源文档、58,184 个 chunk、228,259 个实体节点和 905,580 条关系边，形成 chunks / entities / relationships 三个 2560 维向量库。
10. 在 FoodScience_KG_final_sharded 的 10 题 retrieval-only 测试中，FAISS HNSW sidecar 将检索总耗时中位数从 Nano 路径的 75.76s 降到 6.59s，降低约 91.3%（约 11.5 倍），且 10 题均稳定召回 10/10 个证据 chunk。
11. 在真实健康饮食问答 DQE-Bench 的 178 道 current-gold 样本、712 条四系统输出中，RAgent-FAISS 的 LLM/Rule 总分达 7.72/7.46，高于 enhanced-md Milvus 的 6.78/4.90 和 baseline-md Milvus 的 5.67/4.74。
12. 从能力拆分看，RAgent-FAISS 的规则证据质量为 2.80/3，较 enhanced-md Milvus 的 1.01/3 提升约 177%，同时 RAgent 在 8 类题型中 Rule 口径 8/8 领先、LLM Judge 口径 6/8 领先。

### 服务器端 FoodScience_KG_final_sharded 规模与检索加速补充

当前服务器端使用的 FoodScience_KG_final_sharded 是面向食品科学领域的大规模单项目知识图谱，目录总大小约 24 GB；图谱覆盖 6,520 篇源文档，切分为 58,184 个文本 chunk，图结构包含 228,259 个实体节点和 905,580 条关系边，并对应 chunks / entities / relationships 三个 2560 维向量库。

从存储结构看，关系向量库规模最大，单文件约 12 GB；实体向量库约 3.3 GB；图结构文件 graph_chunk_entity_relation.graphml 约 3.9 GB，因此接近 90 万条关系高维向量是检索性能优化的主要对象。

当前已通过 FAISS HNSW sidecar 对 entities 和 relationships 向量库做近似索引加速，ef_search=128；在 10 题 retrieval-only 测试中，检索总耗时中位数为 6.59s、P95 为 7.33s，较原始 Nano 路径的 75.76s 降低约 91.3%（约 11.5 倍），较仅关系库 HNSW 的 8.99s 再降约 26.7%。

分阶段看，关系向量检索中位数从原 Nano 的 54.15s 降到 0.018s，降幅约 99.97%；实体向量检索中位数从原 Nano 的 15.16s 降到 0.017s，降幅约 99.9%，相比原 flat 实体索引 2.10s 也降低约 99.2%；10 题均稳定召回 10/10 个证据 chunk，且无缓存命中干扰。
