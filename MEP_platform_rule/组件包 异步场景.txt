异步场景组件包开发
2021-03-30 15:29 由 [员工A] [工号已脱敏] 创建，于 2026-03-16 09:54 由 [员工B] [工号已脱敏] 最后修改。 内容存疑 点我
自定义组件包component必须包含两个文件：config.json、process.py。

config.json文件，用来约定推理组件的入口文件以及入口类，main_file表示入口文件（去掉后缀），main_class表示入口文件中的类名。

{
	"main_file": "process",
	"main_class": "CustomerModel"
}
容器通过config.json找到process.py，然后顺序调用process.py 中 的CustomerModel类的load()、calc()方法。

组件代码开发时，可在load()方法中加载模型以及一些耗时的初始化操作。框架会调用calc()方法并传递业务的请求数据，在calc()中需支持对业务数据的接收、业务逻辑处理（如果业务处理逻辑复杂，此处作为业务逻辑处理入口）以及处理结果的返回。处理流程如下：

#!/usr/bin/env python
# -*- coding:utf-8 -*-
import os
import json
from component.util import logutil
from component import handwrite_recognition

logger = logutil.logger_run
currentdir = os.path.dirname(__file__)
parentdir = os.path.abspath(os.path.join(currentdir, os.pardir))
# 模型文件中model目录
model_path = os.path.join(parentdir, "model")
# 模型文件中data目录
data_path = os.path.join(parentdir, "data")

class CustomerModel:
    # 模型加载
    def load(self):
        logger.info("load model start~")
        self.new_model = handwrite_recognition.Test()
        self.new_model.load_pb(os.path.join(model_path, 'test.pb'))
        logger.info("load model end~")

    # 推理计算
    def calc(self, req_Data):
        # 对输入数据的校验和接收
        logger.info("~~~~~~~~~~~~request of component ~~~~~~~~~~~")
        logger.info(req_Data)
        logger.info('~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~')

        if req_Data is None:
            result_code = 3
            des = "request data is missing."
            logger.info("request data is missing.")
            return self.build_response(result_code, des)

        # 注意：依赖SFS异步框架使用如下方式
        data = req_Data.get('data')
        #  data =  req_Data   # 非SFS异步框架使用该方式
        if data is None:
            result_code = 3
            des = "data is missing."
            logger.info("data is missing.")
            return self.build_response(result_code, des)

        # 后续参数校验类似，业务根据具体需要补充,例如
        fileInfo = data.get('fileInfo')
        fileInfo_0 = fileInfo[0]
        sourceImage = fileInfo_0.get('sourceImage')
        sourcePath = fileInfo_0.get('sourcePath')
        generatePath = fileInfo_0.get('generatePath')
        img_path = os.path.join(sourcePath,sourceImage)


        # 调用推理计算
        calc_res = self.new_model.infer(img_path)
        print(calc_res)

        # 结果存储到sfs
        result_path = os.path.join(generatePath,"gen.json")
        with open(result_path,mode='w',encoding='utf-8') as f:
            json.dump(calc_res,f)


        # 处理无误时，返回0
        result_code = 0
        des = 'success'
        return self.build_response(result_code, des)



    # 封装response
    def build_response(self, result_code, result_des):
        res_raw = {"code": str(result_code), "des": result_des, "length": 0, "content": []}
        res = {"recommendResult": res_raw}

        logger.info("result of calc function:%s" % res)
        return res

# 单元测试用例（上传到管理台时以下测试代码须注释）
# test = CustomerModel()
# test.load()
# req_Data = \
#     {
#         "data":
#             {
#                 "taskId": "100002455",
#                 "action": "create",
#                 "basePath": "/opt/vendorA/businessId /100002455",
#                 "fileInfo": [{
#                            "sourceImage": "test0.jpg",
#                             "sourcePath":"E:\\example\\project\\data\\sourcePath",
#                             "generatePath": "E:\\example\\project\\data\\generatePath"
#             }
#         ]
#     }
#     }
# result = test.calc(req_Data)
# print(result)
 

组件包压缩为zip包上传，压缩时须进入到component内进行压缩，样例：



component.zip
 

（该样例组件包是可直接上传到管理台的）。

 

【同步和异步区别】

MEP支持的同步和异步场景，组件包开发基本相同，区别的地方主要在于接口调用参数，详见以下：异步场景接口说明












异步场景组件包接口说明
2021-03-30 15:29 由 [员工A] [工号已脱敏] 创建，于 2025-02-20 10:33 由 [员工C] [工号已脱敏] 最后修改。 内容存疑 点我
非SFS异步场景接口说明请查看：https://wiki.example.com/internal/async-non-sfs（示例链接，已脱敏）
或者该菜单栏下的子菜单。

3.1 接口描述
业务调用MEP的该接口，可执行对应场景的模型，根据预定的路由规则分发到不同的模型服务，并返回对应的模型执行结果。下面以图像处理为例，介绍异步处理的流程：

采用异步接口，模型服务与业务服务会通过SFS共享图片存储；业务请求中传入一组图片存储位置信息、相应的处理要求，以及处理后结果文件的存放位置；模型服务接受到请求后放入异步队列后台进行处理；业务服务器定期过来查询任务处理情况，待任务处理完成后，业务服务读取结果文件内容并进行后续处理。如图所示：



 

 

 

3.2 接口地址
POST /service

3.3 请求消息
参数定义

参数列表

类型

M/O

字段描述

version

String{1,64}

M

服务协议版本号，目前值为1.0

meta

JSONObject

M

请求元数据，由框架约定，JSON结构体

data

JSONObject

M

请求具体数据，JSON结构体 ,各个业务场景定制

 

meta对象

字段名

类型

M/O

描述

bId

String{1,128}

M

业务标识，比如浏览器、某移动网络类应用（示例，已脱敏）

flowId

String{1,128}

M

业务场景，比如裁剪图片，图片语义理解

subId

String{1,128}

O

子场景标识，可选

uuId

String{1,128}

O

请求唯一标识，可以使用uuid、时间戳

traceId

String{1,128}

O

日志跟踪唯一标识，用于调试问题，如果请求中携带了traceID会把各个部件关联的日志(包含debug级别)输出到统一的文件




data对象，JSON结构体，业务定制内容

字段名

类型

M/O

描述

taskId

string

M

任务唯一标识，后续任务查询也需要使用该字段

action

string

M

动作，“create”表示创建，“query”表示查询

basePath

String

M

任务根目录

fileInfo

List<JsonObject>

O

待处理的图片信息，该内容业务根据需要自定义

动作为”create”必选；动作为“query”时不需要携带

 

fileInfo的字段举例：

字段名

类型

M/O

描述

sourceImage

String

M

图片名称

sourcePath

String

M

原图片存放的根路径

generatePath

String

M

处理后的结果存放根路径

processSpec

List<JsonObject>

M

业务自定义处理的配置信息

 

消息样例

以下接口样例都是业务——>MSG的参数说明

 

POST /service

#创建任务场景
Content Type:
application/json;charset=UTF-8

{
    "version": "1.2",
    "meta": {
        "bId": "businessId",
        "flowId": "imageprocess",
        "uuId": "1502346942471"
    },
    "data": {
        "taskId": "100002455",
        "action": "create",
        "basePath": "/opt/vendorA/businessId /100002455",
        "fileInfo": [
            {
                "sourceImage": "image1.png",
                "sourcePath": "/opt/vendorA/businessId/100002455/sourcepath",
                "generatePath": "/opt/vendorA/businessId/100002455/generatePath",
                "processSpec": []
            }
        ]
    }
}


#查询任务场景
{
    "version": "1.2",
    "meta": {
        "bId": "businessId",
        "flowId": "imageprocess",
        "uuId": "1502346942471"
    },
    "data": {
        "taskId": "100002455",
        "action": "query",
        "basePath": "/opt/vendorA/businessId /100002455"
    }
}
 

3.4  响应消息
参数定义

version

String

M

服务协议版本号

meta

JSONObject

M

响应元数据， JSON结构体，取自请求中meta信息

result

JSONObject

M

返回数据，JSON结构体

 

 

meta对象

字段名

类型

M/O

描述

bId

String

M

业务标识，来源于请求中的meta.bId

flowId

String

M

场景标识，来源于请求中的meta.flowId

subId

String

O

子场景标识，来源于请求中的meta.subId

uuId

String

O

请求唯一标识，来源于请求中meta.uuId

 

 

result对象

字段名

类型

M/O

描述

code

String

M

响应码，0为成功，其他为异常

des

String

M

响应描述

length

String

M

模型执行对应业务响应数，返回的content条数

content

JSONArray

M

响应内容列表，数组。

 

 

响应样例

POST /service


Content Type:
application/json;charset=UTF-8
{
    "version": "1.2",
    "meta": {
        "bId": " browser",
        "flowId": "imageCrop",
        "uuId": "1502346942471"
    },
    "result": {
        "code": "0",
        "des": "success",
        "length": "0",
        "content": []
    }
}
 

响应码取值返回及定义

响应码

场景

描述

0

创建任务/查询任务

1、  如请求为创建任务，返回0表示任务创建成功

2、  如请求为查询任务，返回0表示任务执行完成并成功

1

创建任务

该响应码用于创建任务，返回1表示系统忙需要重试

2

查询任务

该响应码用于查询任务，返回2表示任务正在处理中

3

创建任务/查询任务

1、如请求为创建任务，返回3表示任务创建结束，异常

2、如请求为查询任务，返回3表示任务查询结束，异常

4

查询任务

该响应码用于查询任务，返回4表示没有对应的任务

5

创建任务

该响应码用于创建任务，返回5表示所创建任务已存在，创建了重复任务

