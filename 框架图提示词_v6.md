Academic conceptual research figure, white background, flat 2D vector infographic, publication-quality, highly abstract and distilled, minimal text, concise semantic labeling, balanced visual density, modular panel layout, clean research-paper composition, strong information hierarchy, thin outlines, dotted grouping borders, precise arrows, simple geometric shapes, sparse but meaningful icons, structured flow, balanced whitespace, restrained high-contrast color palette, clean typography, professional and technical but not engineering-heavy.

Aspect ratio: 16:9 landscape, optimized for two-column paper figure width.

Core principle: communicate ideas through modular visual organization, not dense explanation. The figure must feel like a distilled research concept, not a system architecture diagram, product UI, dashboard, poster, or bullet-heavy slide.

Core goal:Create a single academic concept figure showing a professional-document RAG system that converts multimodal documents into structured evidence units with provenance, organizes them as an Entity-Relation-Chunk heterogeneous evidence graph, retrieves through cache-accelerated hybrid vector-plus-graph selection, and produces multi-hop traceable answers. Use a left-to-right composition with 4 clearly separated panels, evenly balanced across the page, with consistent spacing and dotted grouping borders. Each panel should have a short bold title at the top. Use thin directional arrows between panels to show data flow and transformation.

Panel 1 — "Structured Ingestion" (green):Show multimodal professional-document inputs on the left with abstract icons for PDF, Image, Table, and Wide Table. From these inputs, draw transformation arrows to structured evidence units on the right. Show four compact processing motifs stacked vertically:

"AST" — Markdown AST-aware chunking (tree-like motif splitting into blocks)

"Row Split" — Wide table row-wise decomposition into independent evidence rows (show a wide table icon breaking into row cards)

"Image Grounding" — Image description with visual grounding (image icon with a text description card)

"Source Tags" — Provenance metadata attachment: page range, section path, source reference (show a tag/badge motif attached to each output unit)The output of this panel should be a stack of structured evidence cards, each carrying a source tag badge.

Below the evidence card stack, show two parallel build lanes labeled "Export" and "Replay" converging into a compact commit gate. This represents the concurrent extraction pipeline: fresh export and offline replay can run in parallel, with a controlled commit point before evidence enters the graph. Add a small circular replay arrow near the commit gate to suggest resumable and revision-aware construction.

Allowed labels in this panel:"PDF", "Image", "Table", "Wide Table", "AST", "Row Split", "Image Grounding", "Source Tags", "Export", "Replay", "Commit"

Panel 2 — "ERC Evidence Graph" (blue for Entity/Chunk, purple for Relation):The central and visually richest panel. Show a heterogeneous graph with three clearly differentiated node types connected by edges:

Entity nodes (blue circles, labeled "Entity") — semantic concepts with type and description

Relation edges (purple directed links, labeled "Relation") — typed relationships with keywords and weight

Chunk nodes (blue rounded rectangles, labeled "Chunk") — original evidence blocks carrying provenance tagsShow that Entity, Relation, and Chunk are explicitly connected into one traceable evidence network. Use a small node-link diagram with 4-6 nodes of each type.Below the graph, show three parallel vector index icons (small database cylinders or index cards), labeled "Entity VDB", "Relation VDB", "Chunk VDB", indicating that each node type has its own dedicated vector index for retrieval.

Allowed labels in this panel:"Entity", "Relation", "Chunk", "Entity VDB", "Relation VDB", "Chunk VDB"

Panel 3 — "Hybrid Retrieval" (orange for retrieval/fusion, red for coverage selection):Show a query entering from the left, then decomposing into 2-3 compact "Query Variants" (small query cards). From these variants, show four parallel retrieval lanes:

Chunk Vector Retrieval (blue lane)

Entity Vector Retrieval (blue lane)

Relation Vector Retrieval (purple lane)

Graph Neighborhood Expansion (purple lane, with a small graph expansion motif showing seed nodes spreading to neighbors)

Near the query variant path, place a compact keyword cache chip. Show two paths from the cache chip: a short fast-hit arrow that reuses cached candidate cards (bypassing vector search), and a longer miss path that continues into the retrieval lanes. The cache motif should be visible and clear but not dominate the panel.

The four retrieval lanes merge into a "RRF Fusion" block (Reciprocal Rank Fusion), then flow into a "Rerank" block, then into a "Coverage Selection" block (highlighted in red). The Coverage Selection block should subtly indicate per-variant coverage guarantee, source diversity, and signal quality filtering. Finally, produce a distilled "Traceable Context" output on the right.

Allowed labels in this panel:"Query Variants", "Keyword Cache", "Chunk Search", "Entity Search", "Relation Search", "Graph Expansion", "RRF Fusion", "Rerank", "Coverage"

Panel 4 — "Traceable QA" (purple for traceability):Represent the upper-layer QA and inspection system abstractly, not as a realistic UI. Use a clean composition with two sub-regions:Upper sub-region — "Multi-Hop Reasoning": show a question being decomposed into sub-questions (question icon with 2-3 sub-question cards), with iterative solving arrows forming a compact reasoning loop.Lower sub-region — "Provenance Trace": show a compact trace chain as a horizontal sequence of linked micro-blocks: Graph Hits → Vector Candidates → Merged → Reranked → Final Evidence → Answer. Use synchronized highlighting (same color accents) between the answer block, evidence chunks, and graph nodes to communicate that every answer is traceable back to its source.

Allowed labels in this panel:"Sub-Questions", "Multi-Hop", "Trace", "Evidence", "Answer"

Main conceptual message:Multimodal professional documents are converted into structured evidence units with provenance tags through concurrent export-replay pipelines, organized as an Entity-Relation-Chunk heterogeneous evidence graph with dedicated per-type vector indexes, queried through cache-accelerated joint vector-plus-graph retrieval with RRF fusion and coverage-aware evidence selection, and finally answered through multi-hop reasoning with full provenance traceability.

Visual language:

use abstract shapes, node-link motifs, layered blocks, compact cards, arrows, loops, chains, and grouped regions

use repeated visual grammar across modules (same card shape, same arrow style, same node conventions)

use text only for short headers and minimal annotations

prefer symbolic structure over literal illustration

make the figure feel distilled, intellectual, and schematic

Color style:Use restrained but clear color coding: green for structured ingestion and build pipeline, blue for entities and chunks, purple for graph relations and traceability, orange for retrieval/fusion/rerank, red for coverage selection emphasis, and neutral gray/black for connectors. Colors should be semantically separated but visually harmonious. Avoid neon tones, gradients, glossy rendering, or decorative effects.

Typography:Use only short section titles and sparse micro-labels. Approximate label count: ~30 total across the figure. No sentence labels, no bullet lists, no code-level terms inside the figure. Do not show labels such as SQLite, MEP, Qwen3, GLiNER, NanoVectorDB, corpus_revision, source_chunk_ids, page_number_start, index_digest, vLLM, or Transformers.

Negative constraints:no photorealismno 3Dno glossy UIno cartoon styleno realistic scenesno device mockupsno screenshotsno dense engineering blueprintno overloaded labelsno poster-like decorationno crowded workflow chartno heavy shadowsno gradient fillsno decorative icons unrelated to the conceptno deployment diagramno cache stack internalsno runtime packaging details

Desired impression:elegant, abstract, modular, highly distilled, visually balanced, concept-driven, research-paper quality, easy to scan, intellectually rich but text-light. A reader should grasp the four-stage pipeline in under 30 seconds: structured evidence is built from multimodal documents through concurrent pipelines, grounded in an ERC heterogeneous graph with per-type indexes, selected through cache-accelerated hybrid retrieval with coverage guarantees, and used to produce multi-hop traceable answers.