Create an image:

Academic conceptual research figure with model-architecture detail, white background, flat 2D vector infographic, publication-quality, abstract but technically informative, clean research-paper composition, modular panel layout, concise semantic labeling, balanced visual density, thin outlines, dotted grouping borders, precise arrows, compact formulas, simple geometric network blocks, sparse but meaningful icons, structured flow, restrained high-contrast color palette, clean typography, professional biomedical AI style.

Important goal:
Create a publication-ready framework figure for an ECG-centered digital physiological phenotyping system. The figure should be more detailed than a high-level concept diagram and should preserve key model architecture information, but still avoid looking like a dense engineering blueprint. Use compact neural-network motifs, token blocks, attention modules, latent vectors, masking symbols, conditional heads, statistical scan panels, and evaluation outputs.

Overall composition:
Create one single integrated figure with 5 connected modules arranged left-to-right, plus a thin bottom training/evaluation strip. Use panel labels A–E. Each panel should be visually distinct but connected by a continuous information flow:
A. Data & QC
B. ECG Representation Backbone
C. Health Anchor Fusion
D. Masked Phenotype Pretraining
E. Physio-Omics X-WAS

Use a moderate level of diagrammatic detail. Include selected short formulas as small labels, not paragraphs. Maintain balanced whitespace and consistent alignment.

Panel A: Data & QC
Show an ECG-centered input layer.
Include two main blue ECG input branches:
1. Raw ECG waveform, single-lead or multi-lead, segmented into short windows.
2. Structured ECG machine features, including QRS, Q/R/S/P morphology, duration, amplitude, area, axis, onset, offset, and peak time.

Add a muted optional branch for PPG or other physiological signals, clearly secondary and not dominant.

Add a gray QC gate before modeling:
- signal quality
- lead availability
- sampling rate
- window segmentation
- subject-level split
- missingness
- batch and time alignment

Represent HPCDE as a gray hierarchical metadata scaffold:
Platform → Assay → Variable
Use small stacked blocks to suggest 21 platforms, 78 assays, and thousands of phenotype variables, without writing large numbers prominently.

Panel B: ECG Representation Backbone
Show a compact neural architecture for ECG-derived representation learning.
For raw ECG:
Raw ECG windows → Signal tokenizer → 1D CNN / temporal encoder / transformer backbone → window embeddings.
Label the window embedding as:
d_t = Encoder(ECG_t)

For structured ECG features:
Structured ECG table → feature normalization → tabular encoder / MLP → exam embedding.
Label as:
d_exam = Encoder(x_ECG_structured)

Merge both paths into a person-level representation through pooling:
mean / median / quality-weighted pooling / attention pooling
Label:
d_person = Pool({d_t}) or d_exam

Show a small self-supervised pretraining motif inside this panel:
masked reconstruction + same-person contrastive learning
Use small mask squares and contrastive pair arrows, not long text.

Panel C: Health Anchor Fusion
Show health anchors as green compact tokens or cards:
vitals, blood pressure, BMI, echocardiography, cardiac MRI, carotid ultrasound, sleep, body composition.

Show an anchor encoder and three possible fusion mechanisms as small parallel motifs:
1. auxiliary supervision
2. cross-attention fusion
3. alignment / distillation

Connect d_person and health-anchor tokens into a central fusion block:
Fusion / Cross-attention / Distillation

Output:
d_health

Make d_health visually central and slightly larger, because it is the bridge between ECG representation learning and phenotype-scale modeling. Convey that d_health is physiology-aware, interpretable, transferable, and expandable.

Panel D: HPCDE-Scale Masked Phenotype Pretraining
Show the most architecture detail here.
Create a purple phenotype-token transformer module.

Inputs:
- d_health token
- observed phenotype tokens y_observed
- masked phenotype tokens y_masked
- label metadata tokens

Represent label metadata encoder explicitly:
LabelEncoder(name, unit, platform, hierarchy, type) → e_k

Show phenotype tokens arranged by domains:
cardiovascular, sleep, body composition, imaging, metabolic, immune, microbiome, questionnaire

Show some tokens visible and some masked with small mask blocks. Use mask-aware attention lines between tokens.

Inside the module, show:
- shared phenotype transformer
- label embeddings e_k
- conditional prediction heads
- FiLM / adapter / hypernetwork motif
- missingness-aware loss
- selection-generative branch

Add compact formulas:
P(y_masked | d_health, y_observed, label_metadata)
e_k = LabelEncoder(...)
ŷ_k = Head(d_health, e_k)

Show missingness explicitly with a small side branch:
measured? → selection model
value distribution → generative model
Use this to indicate that “not measured” is not treated as negative.

Show curriculum learning as a subtle ladder:
coarse platform labels → fine variables → low-frequency labels

Outputs from Panel D:
- phenotype-enhanced d_health
- d_vec modules
- d_score axes
- predictability boundary map

Represent predictability boundary as a compact red-orange gradient or split map:
proximal predictable domains → weak / distal domains
Make clear that the system maps ECG predictability boundaries rather than claiming universal prediction.

Panel E: Physio-Omics X-WAS
Show d_vec and d_score entering an orange association analysis module as continuous response variables.

Show X as high-dimensional omics blocks:
metabolomics, proteomics, exRNA / nucleic acids, microbiome / metagenome, cell immune features.

Show macro health state m and covariates c as small gray nodes.

Include the main multivariable model as a compact formula:
d_vec ~ X + m + X × m + c

Also include the score model:
d_score ~ X + c

Show hierarchical two-step testing:
Step 1: module-level scan
d_module ↔ X_module
Step 2: within-module feature follow-up + BH-FDR

Represent d-side clustering as Q digital phenotype modules and X-side clustering as K omics modules. Use a small matrix or bipartite module map, not a dense heatmap.

Outputs:
- Physio-Omics association atlas
- state-dependent effects
- cross-omics mechanism axes
- testable hypotheses

Make clear that the output is association and hypothesis generation, not causal proof.

Bottom Training / Evaluation Strip:
Add a thin understated workflow strip spanning the whole figure:
Stage 0 QC & standardization
Stage 1 self-supervised ECG backbone
Stage 2 anchor fusion
Stage 3 label-token initialization
Stage 4 masked phenotype pretraining
Stage 5 missingness correction + curriculum learning
Evaluation: calibration, robustness, ablation, sensitivity analysis, predictability boundary

Keep this strip secondary and visually quiet.

Required short labels:
Use only compact labels such as:
Raw ECG
Structured ECG
Signal tokenizer
1D encoder
Attention pooling
d_person
Anchor encoder
d_health
Label metadata
Label tokens
Masked labels
Phenotype transformer
Conditional heads
Missingness-aware
Selection model
d_vec
d_score
Predictability boundary
X-WAS
X × m
BH-FDR
Association atlas

Visual language:
- Use abstract neural network blocks, token grids, attention lines, latent vector capsules, module clusters, and statistical scan motifs.
- Use repeated visual grammar: rounded cards for inputs, capsules for latent vectors, small square tokens for labels, dotted boxes for domains, arrows for flow, dashed arrows for auxiliary objectives.
- Use formulas sparingly as small architectural labels.
- Show architecture enough to reveal the modeling pipeline, but keep it visually elegant and compressed.
- Avoid excessive bullet text. Prefer icons, token blocks, arrows, and compact labels.

Color style:
Use restrained semantic color coding:
- blue for ECG and representation backbone
- green for health anchors
- purple for phenotype tokens and masked pretraining
- orange for omics and X-WAS
- red for predictability boundary and interaction effects
- neutral gray / black for QC, metadata, covariates, workflow, and evaluation

Avoid neon colors, gradients, glossy rendering, heavy shadows, and decorative effects.

Typography:
Clean academic sans-serif typography. Short section titles and compact labels only. Use small formulas as visual anchors. No paragraphs. No dense explanatory callouts.

Negative constraints:
no photorealism
no 3D
no glossy UI
no cartoon style
no realistic hospital scenes
no device mockups
no screenshots
no product dashboard
no software deployment diagram
no crowded engineering blueprint
no overloaded labels
no poster-like decoration
no causal mechanism proof
no universal disease prediction claim
no PPG-dominant wearable figure
no decorative DNA helix overload
no large text blocks

Desired impression:
An elegant, abstract, technically informative biomedical AI framework figure. It should clearly show the full algorithmic architecture: ECG signal and structured-feature encoding, person-level pooling, health-anchor fusion, label-token masked phenotype pretraining, conditional heads with missingness-aware modeling, digital phenotype modules, predictability boundary mapping, and Physio-Omics X-WAS with state-dependent association analysis. The figure should feel research-paper ready, intellectually rich, visually balanced, and easy to scan.

![figure](gemini_figure/figure.png)